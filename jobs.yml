jobs: #resync the jobs
  - name: nginx-mani
    type: manifest
    steps:
      - GET: nginx-image
      - GET: nginx-params
      - GET: nginx-opts
      
  - name: simple-mani
    type: manifest
    steps:
      - GET: simple-image
      - GET: simple-params
      - GET: simple-opts

  - name: deploy-service-dcl
    type: dclDeploy
    steps:
      - GET: generate-multi-mani-1
      - GET: myDclCluster

  - name: ecs-beta
    type: ecsDeploy
    steps:
      - GET: nginx-mani
      - GET: simple-mani
      - GET: betaReplicas
      - GET: myEcsCluster
      - GET: betaParams
      - GET: betaOpts
      
  - name: dcl-prod
    type: dclDeploy
    steps:
      - GET: ecs-beta
      - GET: prodReplicas
      - GET: myEcsCluster
      - GET: prodParams
      - GET: prodOpts


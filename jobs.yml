jobs: #resync the jobs
  - name: generate-manifest
    type: manifest
    steps:
      - GET: nginx-image
      - GET: simple-image
      
  - name: generate-multi-mani-1
    type: manifest
    steps:
      - GET: simple-image
      - GET: generate-manifest
      - GET: myEcsParams

  - name: deploy-service-dcl
    type: dclDeploy
    steps:
      - GET: generate-multi-mani-1
      - GET: myDclCluster

  - name: deploy-service-ecs
    type: ecsDeploy
    steps:
      - GET: generate-multi-mani-1
      - GET: myEcsCluster


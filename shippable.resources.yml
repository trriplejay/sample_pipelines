resources:
  - name: nginx-image-deploy
    type: image
    flags:
      - release
    integration: "trriplejay docker hub"
    pointer:
      sourceName: "library/nginx"
    seed:
      versionName: stable

  - name: nginx-params-deploy
    type: params
    flags:
      - release
    version:
      params:
        mode: dev
        api_url: localhost:50000

  - name: nginx-opts-deploy
    type: dockerOptions
    flags:
      - release
    version:
      memory: 500

  - name: ecsStaging-deploy
    type: cluster
    flags:
      - release
    integration: "shippable demo ecs"
    pointer:
      sourceName : "staging" #name of the cluster we are deploying
      region: "us-west-2"

  - name: ecsProduction-deploy
    type: cluster
    flags:
      - release
    integration: "shippable demo ecs"
    pointer:
      sourceName: "production"
      region: "us-west-2"


  - name: simple-image-deploy
    type: image
    flags:
      - release
    integration: "trriplejay docker hub"
    pointer:
      sourceName: "trriplejay/simpleserver"
    seed:
      versionName: master.58

  - name: simple-params-deploy
    type: params
    flags:
      - release
    version:
      params:
        FOO: "test foo"
        BAR: "test bar"
        MODE: "dev"
        SELF_URL: "http://localhost:8888"

  - name: simple-opts-deploy
    type: dockerOptions
    flags:
      - release
    version:
      memory: 120
      cpuShares: 18
      portMappings:
        - "80:8888"

  - name: simpleReplicas-deploy
    type: replicas
    flags:
      - release
    version:
      count: 2

  - name: stagingVersion-deploy
    type: version
    flags:
      - release
    seed:
      versionName: "1.0.0"

  - name: prodVersion-deploy
    type: version
    flags:
      - release
    seed:
      versionName: "1.0.0"

  - name: myDdcCluster
    type: cluster
    integration: "shippable test ddc"
    pointer:
      sourceName: "blah"
